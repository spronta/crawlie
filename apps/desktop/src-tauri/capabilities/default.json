{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the crawlie window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
